package yfrp.image2mcskin;

public class Main {
    public static void main(String[] args) {
        /*
        -i <导入的文件位置> -o <导出的文件位置>
         */
        for (int i = 0; i < args.length; i++) {
            if (i < args.length - 1 && args[i].equals("-i")) {

            }
        }
    }
}